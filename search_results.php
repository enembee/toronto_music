<?php

//Search results page from index

//Establish db connection

//Load genres from db into array
$genres = mysqli_query();

if (/*In $genres array */) {
	//Return results by closest to current date
} elseif (/* regex on search criteria to determine if valid postal code */) {
	//Return results by closest to  user's current location
} else {
	//Run query on band name string, return results ordered by date ASC
}

?>